import { configTableType } from "@/shared/types/columnTableTypes";

export function FilteredTable<T extends Record<string, any>>({
  data,
  configTable,
}: {
  data: T[];
  configTable: configTableType;
}) {
    
}
